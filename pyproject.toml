[project]
name = "cyoa"
version = "0.1.0"
description = "Choose Yorwn Adventrue Engine"
readme = "README.md"
authors = [{ name = "Dan Shernicoff", email = "dan@brassnet.biz" }]
requires-python = ">=3.14"
dependencies = ["dykes>=0.5.0"]

[project.scripts]
cyoa = "cyoa.app:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
